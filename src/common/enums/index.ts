export enum UserTypes {
  Vendor = 'vendor',
  Customer = 'customer',
  Admin = 'admin',
}

export enum UserRoles {
  Vendor = 'vendor',
  Customer = 'customer',
  Admin = 'admin',
  Staff = 'staff',
}

export enum TokenType {
  Access = 'access',
  Refresh = 'refresh',
}
