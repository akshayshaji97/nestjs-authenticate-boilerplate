import and from './and';
import or from './or';
import authenticate from './authenticate';
import role from './role';
import permission from './permission';

export { and, or, permission, authenticate, role };
export default { and, or, permission, authenticate, role };
